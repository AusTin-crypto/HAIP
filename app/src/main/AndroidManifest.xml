<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.control_majnu">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Control_majnu">
        <activity android:name=".AboutUs" />
        <activity android:name=".PhotoUpload" />
        <activity android:name=".EditSeeker" />
        <activity android:name=".ShowComments" />
        <activity
            android:name=".SeekerMain"
            android:label="@string/title_activity_seeker_main"
            android:theme="@style/Theme.Control_majnu.NoActionBar" />
        <activity android:name=".SeekerProfile" />
        <activity android:name=".RecruiterProfile" />
        <activity android:name=".RecruiterMain" />
        <activity android:name=".Signup" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>